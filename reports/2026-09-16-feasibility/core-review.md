# Task 1 独立审查

审查日期：2026-09-17（Asia/Shanghai）。范围：`cf1c7ff..93b309c` 的 18 个核心 SDK、测试、构建与类型文件。已读取 brief、交付报告、设计、仓库 AGENTS.md、门户标准入口及 SDK/Demo 契约，并对照实际源码与固定差异；当前这些文件相对 `93b309c` 无额外工作区差异。Demo、文档、工作流和公开发布不作为本 Task 的缺失项。

## 规格符合性结论

**通过。未发现需要阻止 Task 1 接收的规格问题。**

- 同步工厂与框架无关接口符合 brief；导入不会创建会话或下载模型。输出含固定 COCO 17 点、原图坐标、裁剪映射、模型身份、请求/实际后端及分项耗时。
- 人体框按固定官方 expand_crop 路径处理整数端点、30% 扩展、宽高比与边界；无框使用整图。预处理保留 OpenCV 插值量化，DARK 使用高斯平滑、对数响应和二阶细化。奇数尺寸后处理中心遵循官方向偶数舍入，未擅自截断坐标。
- score 保留原始热力图最大响应，包括负值和大于 1 的值，没有改造成可见性概率；名称和骨架固定为 COCO 17 点。
- 后端以单一 executionProviders 参数明确提交；WebGPU 缺 API/适配器时明确失败，没有重试 WASM 或自动换源。ORT 自身部分 CPU 节点分配不等于 SDK 静默替换请求后端。
- 运行和加载共享单在途约束；预取消、会话完成和内核完成边界检查取消。主线程释放等待在途任务，输入和全部输出 tensor 在 finally 清理；Worker 释放终止自身 Worker 并拒绝挂起请求。普通取消等待结果后丢弃，未承诺立即停止硬件内核。
- 模型与像素向 Worker 传输前生成 SDK 自有副本，调用者缓冲区不会被转移。缓存身份含 id/version/sha256，每次 load 回读重新校验摘要与长度，损坏后删除并重取；全部清理限定本 SDK 数据库。
- 构建捆绑模块 Worker，ORT 使用发布目录 URL 加载，没有运行时 npm 裸导入；复制同版本 ORT 资源，dist 不含模型。

## 代码质量结论

**通过。没有确认的阻塞缺陷，也没有需要本轮修复的具体质量问题。**

数学、缓存、图片解码、ORT 执行器和公开生命周期分层清晰。清单复制并冻结，公开耗时返回快照；缓存事务关闭连接；Blob 路径关闭 ImageBitmap；输出 tensor 按对象去重释放。测试既有固定官方参考夹具，也有真实 IndexedDB 语义及 Worker structuredClone 转移断言，并用受控会话验证取消和并发释放，测试目标具有独立行为价值。

## 证据和结论边界

本次为只读代码审查，仅新增本报告，没有修改产品代码，也没有重跑已通过的全套测试。28 项单测、类型检查、构建和 Node 导入结果引用 `task-1-report.md`，不冒充本审查重新执行。

读取了 `reports/2026-09-16-feasibility/sdk-browser.json`：时间 `2026-09-16T16:27:51.754Z`，Chromium `153.0.8010.12`，模型 SHA-256 `7614d17acbe957200a8505e11a4fb8445103f9e44a7087115d8a1ea85f88b1b9`。记录为公开 SDK 的 RGBA+人体框完整链路，WASM/WebGPU × main/worker 各 32 图；高响应点最大坐标误差分别为约 0.00040944px 与 0.00037127px，低于 0.1px 门槛；另含 Blob 和生命周期记录。因此没有把预制 Tensor smoke 当作完整 SDK 验证。

该浏览器报告早于核心最终交付报告，尚不能独立证明最终构建字节与测试产物一致；父任务应将最终构建验收绑定到最终产物后再给出整体完成结论。这是证据交接边界，不是本次发现的代码缺陷。这里的通过仅针对 Task 1 本地首版，不代表全量 AP、移动端、NPU、公开分发或生产环境兼容性。
